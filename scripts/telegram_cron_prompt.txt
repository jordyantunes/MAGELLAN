Produce and send a Telegram progress report for the currently running MAGELLAN experiment (a Soft Actor-Critic RL agent training an LLM to play LittleZoo, using a learning-progress-based goal sampler — see CLAUDE.md for background).

1. Run `uv run scripts/telegram_report.py --raw` from the current directory. This prints JSON with: current episode/step, episodes since last check, test success-rate per goal category (grasp, grow_plants, grow_herbivores, grow_carnivores) with deltas, key SAC training metrics (entropy, alpha, alpha_loss, policy_loss, value_loss), downsampled recent history (~20 points each) for entropy/value_loss/policy_loss/alpha for trend shape, newly-crossed milestones, rule-based "hints" (NaNs, sudden entropy drops, value_loss spikes, SR regressions, stuck SR head), a ready-made "metrics_block" (plain text of the current numbers), "prior_checks" (numeric snapshots from the last several checks), and "previously_sent_messages" (the last few messages already sent to the user).
2. Interpret using SAC/RL domain knowledge: entropy near 0 = policy collapse; alpha/alpha_loss decaying is normal as entropy target is met; policy_loss trending negative is expected, sustained explosions are not; value_loss should generally decrease/settle, transient self-correcting spikes are not alarming; test/* metrics are the real measure of goal-learning progress; sr_impossibles staying high after many episodes means the SR head isn't learning per-goal features. Look at the trend arrays and prior_checks, not just the latest values, to judge whether a hint is a transient blip or a real regime change.
3. Compare against previously_sent_messages: do NOT repeat facts/observations that are unchanged from recent messages (e.g. don't re-announce "grasp is solved" every cycle once it's been said). Only call out what's new, changed, or a genuinely recurring concern worth a brief restatement.
4. Compose the Telegram message as: the metrics_block text verbatim first (so raw numbers are always exact, never re-typed or paraphrased), then a blank line, then 3-6 short bullet points (using "•") giving the plain-English interpretation — what's newly learned/changed, and any noteworthy training-stability observations. Don't manufacture concern if nothing changed; a short "steady, nothing new" bullet is fine.
5. Send it with `echo "<message>" | uv run scripts/telegram_report.py --send` (use a heredoc or temp file if the message has characters awkward for inline shell strings). send_telegram sends plain text, no markdown parsing needed. --send also logs the message for future previously_sent_messages context, so this step must actually run.

Report back one line confirming what was sent.
